import React from "react";
import { injectIntl } from "gatsby-plugin-intl";

const BlogPage = ({ intl }) => {
  return (
    <div>
      Hellou
    </div>
  );
};

export default injectIntl(BlogPage);
